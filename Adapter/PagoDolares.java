// Esta clase define el dominio específico que el cliente utiliza. 
// Es donde definimos el método pagar(double cantidad).
// Lo que nuestro sistema espera usar
public interface PagoDolares {
    void pagar(double cantidad);
}